=== DATABASE SCHEMA ===

patients(patient_id*:str, name:str, date_of_birth:dat)
appointments(appointment_id*:str, patient_id:str, appointment_date:dat, appointment_type:str, actual_wait_minutes:int, duration_minutes:int)

JOINS:
  patients.patient_id -> appointments.patient_id

RULES:
- Use T-SQL syntax
- Key columns marked with *
- Types: str=string, int=integer, num=decimal